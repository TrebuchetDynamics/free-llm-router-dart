name: free_llm_router
description: Dart package for routing chat completions to free/no-auth LLM providers.
version: 0.1.0
publish_to: none

executables:
  free_llm_router: free_llm_router

environment:
  sdk: ^3.10.0

dependencies: {}

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
